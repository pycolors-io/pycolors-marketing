---
title: Building a SaaS layout
description: Compose PyColors UI primitives into a scalable, production-ready SaaS layout.
lastUpdated: "2026-04-27"
appliesTo: Guides
---

import { Callout } from "fumadocs-ui/components/callout";
import { Steps, Step } from "fumadocs-ui/components/steps";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import { Badge, Card, CardHeader, CardTitle, CardContent } from "@pycolors/ui";
import { Blocks, LayoutDashboard, LayoutPanelTop, Monitor, PanelLeft, PanelsTopLeft, Smartphone, Settings, Table, CheckCircle, XCircle } from "lucide-react";
import { PreferAvoid } from '@/components/docs/prefer-avoid';
import { DocsFeatureGrid } from "@/components/docs/docs-feature-grid";
import { DocsConceptTabs } from "@/components/docs/docs-concept-tabs";

<div className="flex flex-wrap items-center gap-2">
  <Badge variant="outline">Guides</Badge>
  <Badge>SaaS layout</Badge>
</div>

## Overview

Most UI libraries stop at components.

Real SaaS products require structure:

- navigation
- header
- content orchestration
- responsive behavior
- long-term scalability

This guide shows how to compose PyColors primitives into a **real product layout** that survives growth.

<Callout title="Core idea">
  Layout is not a component. It is a composition of responsibilities that evolve over time.
</Callout>

## What you’re building

<DocsFeatureGrid
  columns={3}
  tone="platform"
  items={[
    {
      title: 'Navigation',
      description:
        'Persistent desktop sidebar with adaptive navigation patterns for smaller screens.',
      icon: PanelsTopLeft,
    },
    {
      title: 'Context',
      description:
        'Headers provide orientation, actions, breadcrumbs, and workspace-level controls.',
      icon: LayoutPanelTop,
    },
    {
      title: 'Content',
      description:
        'Flexible surfaces designed for dashboards, data-heavy views, and product configuration flows.',
      icon: Blocks,
    },
  ]}
/>

### Mental model

```tsx title="conceptual-layout.tsx"
<AppShell>
  <Sidebar />
  <Main>
    <Header />
    <Content />
  </Main>
</AppShell>
```

This is not an API. It is a separation of concerns:

- navigation → where users go
- header → where users understand context
- content → where users act

## Steps

<Steps>

<Step>
### Define layout responsibilities

Do not start with components. Start with responsibilities:

- navigation (global)
- context (page-level)
- content (feature-level)

This prevents premature abstractions.
</Step>

<Step>
### Build desktop-first navigation

Desktop layout should:

- keep sidebar visible
- preserve vertical scanning
- support dense views (tables, dashboards)

Mobile layout should:

- move navigation into a Sheet
- prioritize content
- expose navigation via a trigger
</Step>

<Step>
### Keep header lightweight

Header should contain:

- page title
- optional description
- primary action
- user menu

Avoid turning the header into a control panel.
</Step>

<Step>
### Support multiple content types

Your layout must handle:

- data views (tables, lists)
- settings flows (forms, tabs)
- modal interactions (dialogs)

A good layout does not change when content changes.
</Step>

</Steps>

## Core primitives

| Primitive | Role |
|----------|------|
| Button | actions and triggers |
| Sheet | mobile navigation |
| Dropdown Menu | contextual actions |
| Tabs | settings navigation |
| Table | structured data |
| Dialog | confirmations |

<Callout type="info" title="Key principle">
  You don’t need a “layout system”. You need reliable primitives and good composition.
</Callout>

### Responsive strategy

<DocsConceptTabs
  items={[
    {
      value: 'Desktop',
      title: 'Desktop layout',
      icon: Monitor,
      description: (
        <>
          <p>
            Desktop layouts prioritize persistent navigation and higher information density.
          </p>

          <div className="mt-3 flex flex-wrap gap-2">
            <Badge variant="outline">Sidebar visible</Badge>
            <Badge variant="outline">Persistent navigation</Badge>
            <Badge variant="outline">Dense layout</Badge>
            <Badge variant="outline">Full-height views</Badge>
          </div>
        </>
      ),
    },
    {
      value: 'Mobile',
      title: 'Mobile layout',
      icon: Smartphone,
      description: (
        <>
          <p>
            Mobile layouts prioritize focused content and progressive navigation disclosure.
          </p>

          <div className="mt-3 flex flex-wrap gap-2">
            <Badge variant="outline">Sidebar hidden</Badge>
            <Badge variant="outline">Sheet navigation</Badge>
            <Badge variant="outline">Header trigger</Badge>
            <Badge variant="outline">Content-first layout</Badge>
          </div>
        </>
      ),
    },
  ]}
/>

### Real product examples

#### Data page

- Table
- Filters
- Row actions
- Pagination

#### Settings page

- Tabs
- Forms
- Dialogs
- Validation

Same layout. Different content.

## Decision guide

<PreferAvoid
  prefer={[
    'composition over abstraction',
    'clear separation of concerns',
    'mobile-first adaptability',
    'small reusable primitives',
  ]}
  avoid={[
    'monolithic AppShell too early',
    'hardcoded layout logic',
    'coupling navigation with content',
    'over-engineered abstractions',
  ]}
/>

## When to introduce an AppShell

Create your own AppShell when:

- layout repetition becomes real
- multiple teams contribute
- navigation complexity grows
- consistency becomes critical

Not before.

### Summary

- Layout is composition, not a component
- Keep responsibilities separated
- Use primitives, not magic abstractions
- Scale structure only when needed

<Callout title="Next step">
  Combine this layout with async states and forms to reach production-level UX.
</Callout>
