---
title: Auth system (PRO)
description: Production-ready authentication with real flows, OAuth, session handling, and security foundations for real SaaS products.
lastUpdated: "2026-05-18"
appliesTo: Starter Pro
---

import { Callout } from "fumadocs-ui/components/callout";
import { Accordion, Accordions } from "fumadocs-ui/components/accordion";
import { Badge } from "@pycolors/ui";
import {
  CreditCard,
  Database,
  Github,
  KeyRound,
  LockKeyhole,
  MailCheck,
  Package,
  RefreshCcw,
  ShieldCheck,
  UserRoundCheck,
} from "lucide-react";

import { PreferAvoid } from "@/components/docs/prefer-avoid";
import { DocsDecisionGrid } from "@/components/docs/docs-decision-grid";
import { DocsFeatureGrid } from "@/components/docs/docs-feature-grid";
import { DocsLinks } from "@/components/docs/docs-links";
import { DocsUpgradeCTA } from "@/components/docs/docs-upgrade-cta";

<div className="flex flex-wrap items-center gap-2">
  <Badge variant="outline">Starter Pro</Badge>
  <Badge>Auth</Badge>
</div>

## Authentication that actually ships

Starter Free gives you **auth screens and UX states**.

Starter Pro gives you **a production-ready authentication system** for real SaaS products:

- real users
- real sessions
- real providers
- real token flows
- real protected routes
- real account lifecycle foundations

This is the layer that turns login pages into a working product system.

<DocsUpgradeCTA
  title="Ship auth once. Ship it right."
  description="The cost of weak auth is usually paid later. Starter Pro helps you avoid that by starting from a stronger foundation."
  productSlug="starter-pro"
  buyLabel="Get Starter Pro"
  secondaryHref="/docs/starter-pro/getting-started"
  secondaryLabel="Setup guide"
/>

<Callout title="Core idea">
  Auth is not UI. Auth is lifecycle, security, state, redirects, provider rules, and trust.
</Callout>

## What this solves

Authentication is usually one of the first serious blockers before launch.

It looks simple at the UI level:

- login
- register
- forgot password
- OAuth buttons

But production auth also needs:

- password hashing
- session persistence
- email verification
- password reset tokens
- provider account linking
- protected route behavior
- safe disconnect rules
- rate limiting foundations
- secure error handling

Starter Pro gives you a structured foundation for that layer so you do not rebuild the full lifecycle from scratch.

### What is included

<DocsFeatureGrid
  columns={2}
  tone="platform"
  items={[
    {
      title: 'Credentials auth',
      description:
        'Email and password authentication with account creation, password validation, password reset, and password change foundations.',
      icon: LockKeyhole,
    },
    {
      title: 'OAuth providers',
      description:
        'Google and GitHub OAuth foundations with connected account patterns and safer provider lifecycle handling.',
      icon: Github,
    },
    {
      title: 'Verification and tokens',
      description:
        'Email verification, reset password, token expiration, and recovery flows structured for production use.',
      icon: MailCheck,
    },
    {
      title: 'Protected foundations',
      description:
        'Session-aware UI, protected routes, auth-aware backend logic, and account safety patterns.',
      icon: ShieldCheck,
    },
  ]}
/>

### Auth flows

<DocsDecisionGrid
  columns={2}
  items={[
    {
      eyebrow: 'Sign in',
      title: 'Sign in flow',
      icon: LockKeyhole,
      description:
        'A production sign-in flow needs more than a form. It needs state, errors, redirects, and session creation.',
      items: [
        'email and password sign in',
        'OAuth sign in',
        'session creation',
        'error-aware UX',
        'redirect to protected app surface',
      ],
    },
    {
      eyebrow: 'Register',
      title: 'Registration flow',
      icon: UserRoundCheck,
      description:
        'Registration turns anonymous users into real accounts with a lifecycle that can support verification and onboarding.',
      items: [
        'real account creation',
        'password validation',
        'email verification-ready lifecycle',
        'provider signup path',
        'session-aware onboarding path',
      ],
    },
    {
      eyebrow: 'Password',
      title: 'Password lifecycle',
      icon: KeyRound,
      description:
        'Password flows need secure recovery, expiration-aware tokens, and clear account safety rules.',
      items: [
        'forgot password request',
        'reset password token',
        'expiration-aware recovery flow',
        'in-session password change',
        'add password for OAuth-only users',
      ],
    },
    {
      eyebrow: 'Providers',
      title: 'Connected accounts',
      icon: RefreshCcw,
      description:
        'Provider flows need connection, linking, disconnect, and fallback behavior so users do not get locked out.',
      items: [
        'Google account connection',
        'GitHub account connection',
        'provider linking patterns',
        'safe disconnect rules',
        'account fallback behavior',
      ],
    },
  ]}
/>

## Starter Free vs Starter Pro

| Capability | Starter Free | Starter Pro |
| --- | --- | --- |
| Auth screens | Included | Included |
| Credentials auth | Mocked | Real |
| Google OAuth | Not included | Real |
| GitHub OAuth | Not included | Real |
| Sessions | Mocked / partial | Real |
| Email verification | Not included | Real |
| Forgot password | UI only | Real flow |
| Reset password | UI only | Token-based |
| In-session password change | Not included | Included |
| OAuth account linking | Not included | Included |
| Safe provider disconnect rules | Not included | Included |
| Protected routes | Conceptual | Production-ready foundation |
| Rate limiting foundations | Not included | Included pattern |

<Callout title="Simple distinction">
  Starter Free simulates auth. Starter Pro handles real users.
</Callout>

## Architecture

### Data model responsibilities

Starter Pro auth is designed around the structures a serious SaaS usually needs.

Typical auth responsibilities include:

| Area | Purpose |
| --- | --- |
| User | Identity, email, profile image, verification state |
| Account | OAuth provider accounts and account linking |
| Session | Active user sessions |
| VerificationToken | Auth.js-compatible verification flows |
| UserToken | Email verification and password reset lifecycle |
| AuthAttempt | Audit and rate-limiting foundation |

<Callout type="info" title="Why this matters">
  Production auth is not only about signing in. It is about storing enough state to make account lifecycle, recovery, and provider logic safe.
</Callout>

### Protected routes

Auth becomes valuable when it protects real product surfaces.

Starter Pro is designed so the auth layer can protect:

- dashboard
- settings
- billing
- admin
- customer data
- plan-aware product areas

```txt title="protected-routes.txt"
/dashboard
/settings
/billing
/admin
```

A good protected route system should:

- redirect anonymous users
- preserve safe app boundaries
- expose session-aware UI
- avoid leaking private state
- work with billing and backend ownership rules

### Integration with billing

Auth and billing are connected in a real SaaS.

A customer is not just a payment record. A customer belongs to a user or organization.

That means auth must support:

- account identity
- customer ownership
- subscription access
- billing portal access
- plan-aware feature gating

<Callout title="Why auth matters for revenue">
  You cannot reliably charge customers until you can reliably identify them.
</Callout>

### Security rules

Use these rules when adapting the authentication layer.

<PreferAvoid
  prefer={[
    'server-side validation for authentication actions',
    'short-lived tokens for recovery flows',
    'generic forgot-password confirmation copy',
    'clear session-aware redirects',
    'provider disconnect rules that never lock users out',
  ]}
  avoid={[
    'revealing whether an email exists',
    'allowing users to disconnect their only login method',
    'trusting client-side authentication state for protected data',
    'returning overly specific authentication errors',
    'mixing authentication logic into unrelated product components',
  ]}
/>

## Decision guide

<DocsDecisionGrid
  columns={2}
  items={[
    {
      eyebrow: 'Starter Pro',
      title: 'Use Starter Pro auth when auth becomes real',
      href: '/docs/starter-pro/getting-started',
      icon: ShieldCheck,
      tone: 'pro',
      description:
        'Use Starter Pro when authentication is part of the launch foundation and not only a mocked screen.',
      items: [
        'real users need accounts',
        'sessions must persist',
        'OAuth providers matter',
        'password reset must work',
        'routes need protection',
        'billing depends on identity',
        'account lifecycle edge cases matter',
      ],
      cta: 'Start setup →',
    },
    {
      eyebrow: 'Starter Free',
      title: 'Stay on Free while validating UX',
      icon: UserRoundCheck,
      description:
        'Stay on Starter Free when you are still testing the product surface and do not need real users yet.',
      items: [
        'you are still validating UX',
        'you do not need real users yet',
        'auth is not blocking progress',
        'you are not preparing monetization',
      ],
    },
  ]}
/>

<Callout title="Upgrade when auth becomes real">
  Starter Free helps you design the auth surface. Starter Pro gives you the auth system behind it.
</Callout>

### Implementation checklist

Before treating auth as launch-ready, validate:

- credentials sign in
- OAuth sign in
- registration
- email verification
- forgot password
- reset password
- password change
- protected route redirects
- session-aware navigation
- connected account behavior
- provider disconnect rules
- billing access ownership

<Callout type="info" title="Validation principle">
  Do not customize deeply before validating the default flows.
  Confirm the foundation first, then adapt the product.
</Callout>

## Common questions

<Accordions type="single">
  <Accordion title="Is this just UI around auth?">
    No. Starter Pro focuses on real auth behavior: sessions, providers, tokens, protected routes, and account lifecycle structure.
  </Accordion>

  <Accordion title="Can I remove a provider?">
    Yes. Providers should be treated as configurable entry points. Keep safe account fallback rules if you allow users to disconnect providers.
  </Accordion>

  <Accordion title="Should billing depend on auth?">
    Yes. In a SaaS, billing state must be connected to a user, customer, or organization identity. Auth is the foundation for that ownership.
  </Accordion>

  <Accordion title="Can I replace the auth provider later?">
    You can, but auth touches many parts of a product. Replace it deliberately and keep the same boundaries around sessions, tokens, and protected access.
  </Accordion>
</Accordions>

<DocsUpgradeCTA
  title="Authentication is launch infrastructure."
  description="Move to Starter Pro when login pages need to become a secure account system with sessions, providers, recovery, and protected access."
  productSlug="starter-pro"
  buyLabel="Get Starter Pro"
  secondaryHref="/pricing"
  secondaryLabel="View pricing"
/>

## Next steps

<DocsLinks
  variant="grid"
  items={[
    {
      title: 'Billing system',
      href: '/docs/starter-pro/billing',
      description:
        'Connect authenticated users to customers, subscriptions, and plan-aware access.',
      icon: CreditCard,
    },
    {
      title: 'Backend foundations',
      href: '/docs/starter-pro/backend',
      description:
        'Understand how authentication integrates with data ownership and server-side logic.',
      icon: Database,
    },
    {
      title: 'Production checklist',
      href: '/docs/starter-pro/production-checklist',
      description:
        'Validate authentication, billing, backend, and delivery before launch.',
      icon: ShieldCheck,
    },
    {
      title: 'What is included',
      href: '/docs/starter-pro/what-is-included',
      description:
        'Review the full Starter Pro foundation.',
      icon: Package,
    },
  ]}
/>
