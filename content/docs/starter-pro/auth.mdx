---
title: Auth system (PRO)
description: Production-ready authentication with real flows, OAuth, session handling, and security foundations for real SaaS products.
---

import Link from 'next/link';
import { DocsPageShell } from "@/components/shells/docs-page-shell";
import { Badge, Button } from "@pycolors/ui";
import { BuyStarterProButton } from "@/components/pricing/buy-starter-pro-button";

<DocsPageShell>

<Badge>Starter Pro</Badge>

# Authentication that actually works in a real SaaS

Starter Free gives you **auth screens and UX states**.

Starter Pro gives you **real authentication, real sessions, real providers, and the security foundations required before launch**.

This is not just login UI.

It is the part that turns “auth screens” into a working product system for real users.

<div className="mt-6 flex flex-col gap-3 sm:flex-row">
 <BuyStarterProButton fullWidth={false} showTrustText />
  <Button asChild variant="outline" className="h-11 rounded-xl px-6 text-sm font-medium no-underline">
    <Link href="/starters/pro">
      View Starter Pro
    </Link>
  </Button>
</div>

---

## What this includes

The Starter Pro auth system is a **production-ready authentication layer**.

You get:

- email and password authentication
- Google OAuth
- GitHub OAuth
- email verification flow
- forgot password
- reset password
- in-session password change
- add password for OAuth-only users
- connected accounts management
- safer provider disconnect rules
- security activity patterns
- rate limiting foundations

Everything is already structured so you can move from auth UI to real auth behavior faster.

---

## Real flows, not placeholders

This is not simulated UX.

### Sign in

- credentials auth
- OAuth providers
- session-aware behavior
- real error handling

### Register

- real account creation
- password validation
- provider signup
- verification-ready flow

### Password lifecycle

- forgot password
- reset password
- token-based recovery
- expiration-aware flow

### Account security

- connected provider handling
- safer unlink logic
- password management
- account lifecycle structure

---

## What changes vs Starter Free

Starter Free gives you:

- auth pages
- auth UX
- navigation states
- product-shaped flows

Starter Pro adds:

- real sessions
- real providers
- real token lifecycle
- real database integration
- real security foundations
- real rate-limiting patterns

> Starter Free simulates auth.
> Starter Pro handles real users.

---

## Why this matters

Authentication is not just a small feature.

It is:

- your first security layer
- your first user trust layer
- your first major production risk

Most starters stop at screens.

You still have to build:

- token systems
- provider linking
- session behavior
- auth edge cases
- safe provider handling
- security rules

That is where time disappears before launch.

And that is exactly why Starter Pro exists.

---

## What you avoid building yourself

Without Starter Pro, you usually have to implement:

- password hashing and validation
- token lifecycle for email verification and reset
- OAuth linking logic
- session-aware auth behavior
- account edge cases
- safe provider disconnect logic
- rate limiting and safer auth handling

This layer is:

- security-sensitive
- easy to partially get wrong
- expensive to rebuild later

<div className="mt-6">
  <BuyStarterProButton showTrustText />
</div>

---

## How it integrates with the rest of the product

The auth layer is not isolated.

It connects directly to:

- protected routes
- dashboard access
- settings pages
- subscription-aware behavior
- backend ownership rules

That matters because real auth only becomes valuable when it works with the rest of the product.

---

## When you need this

Move to Starter Pro auth when:

- users need to sign in for real
- you want real onboarding
- you need real sessions
- you prepare monetization
- you want production security, not just auth UI

A good rule is simple:

Stay on Starter Free when your question is:

> “How should auth feel?”

Move to Starter Pro when your question becomes:

> “How do I launch auth without rebuilding the whole lifecycle?”

---

## The payoff

Instead of spending days or weeks wiring auth:

- you configure providers
- you connect environment variables
- you validate flows
- you ship faster

That is the value.

---

## Mental model

> Starter Free gives you the auth surface.
> Starter Pro gives you the auth system.

---

## Next steps

Authentication is one core layer.

Continue with:

- Billing → `/docs/starter-pro/billing`
- Backend foundations → `/docs/starter-pro/backend`

<div className="mt-6 flex flex-col gap-3 sm:flex-row">
 <BuyStarterProButton fullWidth={false} showTrustText />
  <Button asChild variant="outline" className="h-11 rounded-xl px-6 text-sm font-medium no-underline">
    <Link href="/pricing">
      Compare pricing
    </Link>
  </Button>
</div>

</DocsPageShell>