---
title: Getting Started
description: Understand what Starter Free gives you, what it does not include, and how to ship your first SaaS screen in under 30 minutes.
lastUpdated: "2026-04-27"
appliesTo: Starter Free
---

import { Callout } from "fumadocs-ui/components/callout";
import { Accordion, Accordions } from "fumadocs-ui/components/accordion";
import { Step, Steps } from "fumadocs-ui/components/steps";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import {
  Badge,
  Card,
  CardContent,
  CardHeader,
  CardTitle,
} from "@pycolors/ui";
import {
  ArrowRight,
  ArrowUpRight,
  CheckCircle,
  CreditCard,
  FolderTree,
  LayoutDashboard,
  LockKeyhole,
  Rocket,
  Settings,
  Terminal,
  Users,
  XCircle,
} from "lucide-react";
import { PreferAvoid } from '@/components/docs/prefer-avoid';
import { DocsLinks } from "@/components/docs/docs-links";
import { DocsFeatureGrid } from "@/components/docs/docs-feature-grid";
import { DocsConceptTabs } from "@/components/docs/docs-concept-tabs";

<div className="flex flex-wrap items-center gap-2">
  <Badge variant="outline">Starter</Badge>
  <Badge>Free</Badge>
</div>

## Overview

Starter Free gives you a **credible SaaS surface out of the box**.

Not a blank repo.
Not a backend-heavy template.
Not a toy demo.

It gives you a production-shaped UI with real screens, real states, and real navigation so you can validate your product direction before investing in backend complexity.

The goal is simple:

> run it in minutes, understand it quickly, and adapt your first product surface fast.

<Callout title="Why this matters">
  Most SaaS projects lose time before users ever see the product. Starter Free helps you make the product visible earlier, so you can validate UX, messaging, and flows before wiring the full business layer.
</Callout>

## What you’ll learn

<DocsFeatureGrid
  columns={3}
  tone="platform"
  items={[
    {
      title: 'Product surface',
      description:
        'Understand the screens, layouts, states, and flows included in Starter Free.',
      icon: LayoutDashboard,
    },
    {
      title: 'Fast validation',
      description:
        'Adapt the starter into a first vertical slice without rebuilding the product surface.',
      icon: Rocket,
    },
    {
      title: 'Upgrade path',
      description:
        'Know what is mocked, what is production-ready, and when Starter Pro becomes the next step.',
      icon: LockKeyhole,
    },
  ]}
/>

### Mental model

> Starter Free is a UX contract, not a full backend implementation.

You keep:

- screens
- navigation
- components
- states
- product structure

You replace later:

- mock data
- auth provider
- billing logic
- API calls
- database contracts

<Callout type="info" title="Frontend-first by design">
  Starter Free is intentionally mocked. This lets you evaluate structure and UX first, then wire auth, data, and billing progressively when the product needs it.
</Callout>

## Steps

<Steps>

<Step>
### Understand what is included

Starter Free gives you the main surface area of a SaaS product.

| Surface | What it gives you |
| --- | --- |
| Auth UX | Login, register, forgot password, loading and error-ready states |
| Dashboard | Layout, navigation, KPI cards, and structured product surface |
| Projects | Table, loading state, empty state, rename dialog, delete confirmation, details page |
| Admin | Members list, invitations, roles, and invite workflow |
| Billing | Current plan, invoice table, portal entry point, and payment method surface |
| Settings | Profile, organization, security, and danger zone structure |

This is enough to validate a real product direction before wiring production services.
</Step>

<Step>
### Know what is intentionally mocked

Starter Free does not connect to production services.

It does not include:

- database schema
- API routes
- Stripe integration
- session management
- email provider
- role-based access control

That is intentional.

```txt title="starter-free-boundary.txt"
Starter Free
  UI surface
  product flows
  mock data
  realistic states

Starter Pro
  auth wiring
  billing wiring
  backend foundation
  production flows
```

The goal is to avoid backend friction before you know the product surface is worth wiring.
</Step>

<Step>
### Explore the vertical slice

After installation, inspect the starter as a user would.

Open:

- `/dashboard`
- `/projects`
- `/projects/[id]`
- `/admin`
- `/billing`
- `/settings`

Then test:

- table loading and empty states
- rename dialog
- delete confirmation
- member invitation flow
- billing surface
- settings tabs

This gives you the product logic before you start changing code.
</Step>

<Step>
### Adapt one product surface

Start small. Rename one core entity and adjust the first product flow.

Good first changes:

- rename “Projects” to your domain entity
- update dashboard KPIs
- change empty state copy
- adjust navigation labels
- add one new route
- update brand tokens

This is enough to create a credible demo or first MVP surface.
</Step>

</Steps>

## What Starter Free solves

<DocsConceptTabs
  items={[
    {
      value: 'Auth',
      title: 'Auth UX',
      icon: LockKeyhole,
      description: (
        <>
          <p>
            Starter Free includes the screens and interaction states needed to model authentication before wiring a provider.
          </p>

          <div className="mt-4 flex flex-wrap gap-2">
            <Badge variant="outline">/login</Badge>
            <Badge variant="outline">/register</Badge>
            <Badge variant="outline">/forgot-password</Badge>
            <Badge variant="outline">Error-ready forms</Badge>
            <Badge variant="outline">Password visibility toggle</Badge>
          </div>
        </>
      ),
    },
    {
      value: 'Product',
      title: 'Product surface',
      icon: LayoutDashboard,
      description: (
        <>
          <p>
            The starter provides a real app shell, dashboard structure, CRUD surface, dialogs, details pages, and navigation.
          </p>

          <div className="mt-4 flex flex-wrap gap-2">
            <Badge variant="outline">Dashboard layout</Badge>
            <Badge variant="outline">Projects table</Badge>
            <Badge variant="outline">Row actions</Badge>
            <Badge variant="outline">Details page</Badge>
            <Badge variant="outline">UI states</Badge>
          </div>
        </>
      ),
    },
    {
      value: 'Business',
      title: 'Business-ready surfaces',
      icon: CreditCard,
      description: (
        <>
          <p>
            Starter Free includes mocked billing, admin, members, and settings surfaces so the product already feels commercial.
          </p>

          <div className="mt-4 flex flex-wrap gap-2">
            <Badge variant="outline">Billing overview</Badge>
            <Badge variant="outline">Invoice table</Badge>
            <Badge variant="outline">Members list</Badge>
            <Badge variant="outline">Invite workflow</Badge>
            <Badge variant="outline">Settings tabs</Badge>
          </div>
        </>
      ),
    },
  ]}
/>

### What you can ship quickly

After installation, you can produce a credible first version by changing:

| Task | Outcome |
| --- | --- |
| Rename “Projects” | Makes the starter match your domain |
| Update dashboard KPIs | Makes the product story specific |
| Rewrite empty states | Makes the UX feel intentional |
| Adjust navigation labels | Makes the product easier to demo |
| Change brand tokens | Makes the interface feel owned |
| Add one route | Proves the structure can grow |

That is enough to:

- show a client
- pitch an MVP
- validate UX
- record a product demo
- test your product narrative

## Decision guide

Use Starter Free if:

- you are validating UX
- you are pitching a product
- you are building a frontend-first MVP
- you want product structure before backend
- you need something credible fast

Move to Starter Pro when you need:

- real authentication
- Stripe billing
- backend APIs
- protected routes
- database-backed product flows
- role-based access control
- production delivery

<PreferAvoid
  prefer={[
    'explore the full vertical slice first',
    'adapt one domain entity before rewriting structure',
    'keep mock data while validating UX',
    'wire backend only when the product direction is clear',
    'upgrade when auth, billing, and data become the blocker',
  ]}
  avoid={[
    'treating Starter Free like a complete SaaS backend',
    'rewriting the structure before understanding it',
    'mixing UI and business logic too early',
    'removing product surfaces before testing the full flow',
    'adding backend complexity before validating the product',
  ]}
/>

### First run path

Follow this sequence after cloning:

1. open `/dashboard`
2. open `/projects`
3. rename a project
4. delete a project
5. open a project details page
6. open `/admin`
7. open `/billing`
8. open `/settings`

This path shows the product shape: dashboard, entity workflow, admin surface, billing surface, and settings structure.

<Callout title="Upgrade when wiring becomes the blocker">
  Starter Free validates the product surface. Starter Pro wires the business layer with production-shaped auth, billing, backend, and delivery foundations. → `/docs/starter-pro`
</Callout>

## Common questions

<Accordions type="single">
  <Accordion title="Is Starter Free a full SaaS backend?">
    No. Starter Free is frontend-first and mock-data based. It is designed to validate UX and product structure before production wiring.
  </Accordion>

  <Accordion title="Can I use it for a real MVP?">
    Yes, especially for demos, validation, frontend-first MVPs, and product exploration. For real users, real payments, and protected data, move to Starter Pro or wire your own backend.
  </Accordion>

  <Accordion title="Should I delete the mocked features immediately?">
    No. First explore the flows. The mocked surfaces teach the intended product structure and give you a stronger starting point for your own implementation.
  </Accordion>
</Accordions>

## Next steps

<DocsLinks
  variant="grid"
  items={[
    {
      title: 'Installation',
      href: '/docs/starter/installation',
      description:
        'Run Starter Free locally and explore the real application screens and flows.',
      icon: Terminal,
    },
    {
      title: 'Project structure',
      href: '/docs/starter/project-structure',
      description:
        'Understand how the starter is organized before extending product features.',
      icon: FolderTree,
    },
    {
      title: 'Authentication concept',
      href: '/docs/starter/auth-concept',
      description:
        'Learn how authentication is modeled before connecting a real provider.',
      icon: LockKeyhole,
    },
    {
      title: 'Upgrade to Starter Pro',
      href: '/docs/starter/upgrade',
      description:
        'Know when Starter Free stops being enough and Starter Pro becomes the faster launch path.',
      icon: ArrowUpRight,
      badge: 'Pro',
      tone: 'primary',
    },
  ]}
/>
